version: 1
protected_paths:
  hard:
    - ".github/workflows/**"
  soft:
    - "app/**"
    - "domain/**"
  soft_exceptions: []
review_policy: always
risky_review_threshold: 1
auto_merge: false
checks_pending_timeout: 1h
flaky_retry_limit: 1
